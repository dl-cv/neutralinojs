# Neutralino 运行时交付规则

## Windows 编译与品牌资源

- 原生代码修改后使用根目录 `编译.bat` 编译，确认退出码及 `bin/neutralino-win_x64.exe` 产物。未改原生代码时不重复编译。
- `bin/neutralino-win_x64.exe` 是未处理品牌资源的中间产物，不直接交付业务项目。
- 交付 DLCV Test 前，使用 `换图标、添加版本信息.bat test "<目标工作区>\neu\bin\neutralino-win_x64.exe"`，以仓库已有 `dist/test.ico` 和 `dist/test.rc` 写入测试平台 Logo 与产品信息，并直接覆盖指定目标文件。
- `test` 模式仅处理测试平台，不写其他项目。无参数模式会处理多个项目，单项目任务不得使用该模式。
- 应用项目将更新后的 EXE 与功能改动一并提交。该文件是应用项目已有的受版本管理资源，不是仅供本机使用的外部输入。

## 应用项目编译流程

- DLCV Test 继续使用已有 `neu/bin/neutralino-win_x64.exe`，执行 `11_编译打包.bat`、`12_安装新版.bat`。
- 不新增 `DLCV_NEUTRALINO_BINARY` 等必填环境变量，不要求开发者另行传入运行时，不改写应用项目原有编译入口。
- 不直接修改已安装目录。运行时更新经项目预设打包、安装步骤交付。
- 验证源码目录和安装产物均带测试平台图标及产品信息；窗口截图包含完整标题栏与编辑区。
