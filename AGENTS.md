# Windows 运行时资源

- 原生代码修改后执行根目录 `编译.bat`，检查 `bin/neutralino-win_x64.exe` 是否生成。
- 根目录 `换图标、添加版本信息.bat` 使用仓库内的图标与版本信息生成各应用的运行时文件，并按脚本末尾的路径自动复制到对应项目。
- 目标项目目录变化时，只更新该脚本中的复制路径；新增应用时，在脚本中增加其图标、版本信息和复制目标。不要求应用项目修改编译入口或传入运行时路径。
